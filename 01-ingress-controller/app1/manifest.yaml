apiVersion: apps/v1
kind: Deployment
metadata:
  name: app1
  labels:
    project: roboshop
    tier: frontend
    component: app1
spec:
  replicas: 2
  selector:
    matchLabels:
      project: roboshop
      tier: frontend
      component: app1
  template:
    metadata:
      labels:
        project: roboshop
        tier: frontend
        component: app1
    spec:
      containers:
      - name: app1
        image: joindevops/app1:1.0.0
---
apiVersion: v1
kind: Service
metadata:
  name: app1
spec:
  selector:
    project: roboshop
    tier: frontend
    component: app1
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
---
# OLD ALB Ingress Controller (before AWS Load Balancer Controller)
# Problems:
#   - target-type: instance → ALB → EC2 node (NodePort) → pod (extra hop)
#   - no group.name → every app gets its OWN ALB (expensive)
#   - v1beta1 API → removed in K8s 1.22
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: app2
  annotations:
    kubernetes.io/ingress.class: "alb"
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: instance
spec:
  ingressClassName: alb
  rules:
  - host: "app1.dev88s.online"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: app1
            port:
              number: 80